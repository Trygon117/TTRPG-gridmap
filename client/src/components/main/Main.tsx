import RenderWindow from "../renderWindow/RenderWindow";

const Main = (props) => {

    return (
        <div style={{ border: "5px solid grey" }}>
            <RenderWindow />
        </div>
    );
}

export default Main;